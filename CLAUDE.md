# artifact-share-release

Release distribution repo for the artifact-share MCP server.

Contains `version.json` (the update manifest) and binary assets attached to GitHub releases.

To cut a new release, update `version.json` and create a GitHub release with the new binaries attached.
